---
apiVersion: apps.openshift.io/v1
kind: DeploymentConfig
metadata:
  name: ${env.name}
  namespace: ${namespaceName}
spec:
  replicas: 1
  selector:
    app: ${env.name}
    deploymentConfig: ${env.name}
  template:
    metadata:
      labels:
        namespace: ${namespaceName}
        app: ${env.name}
    spec:
      containers:
      - name: ${env.name}
        image: ${env.name}:${appTag}
        ports:
        - containerPort: 8763
      imagePullPolicy: IfNotPresent
  strategy:
    type: Rolling
  triggers:
  - type: ConfigChange
---
apiVersion: v1
kind: Service
metadata:
  name: ${env.name}
  labels:
    app: ${env.name}
    deploymentConfig: ${env.name}
spec:
  type: NodePort
  ports:
  - port: 8763
    targetPort: 8763
  selector:
    app: ${env.name}
---
